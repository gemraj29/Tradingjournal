# Always install optional dependencies (prevents react-refresh / rollup native binaries dropping silently)
optional=true

# Don't skip optional deps on platform mismatch
omit[]=

# Retry failed fetches
fetch-retries=3
fetch-retry-mintimeout=5000
